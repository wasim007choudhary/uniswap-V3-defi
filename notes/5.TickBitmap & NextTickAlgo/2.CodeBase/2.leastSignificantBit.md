# 🔥 `leastSignificantBit()` — Complete Dissection

Yep. **Now we move directly from `mostSignificantBit()` to `leastSignificantBit()`**, and we’ll dissect it the same way we dissected MSB: line-by-line, with the binary-box mental model, the questions/confusions that come up, concrete examples, child analogies, and then connect it back to the Tick Bitmap `masked` flow.

And **I will not generate the `.md` yet**. We’ll do the actual dissection first. When you later say **“now give me the `.md`”**, I’ll compile the whole journey.

We start here:

```solidity
function leastSignificantBit(uint256 x) internal pure returns (uint8 r) {
    require(x > 0);

    r = 255;
    if (x & type(uint128).max > 0) {
        r -= 128;
    } else {
        x >>= 128;
    }
    if (x & type(uint64).max > 0) {
        r -= 64;
    } else {
        x >>= 64;
    }
    if (x & type(uint32).max > 0) {
        r -= 32;
    } else {
        x >>= 32;
    }
    if (x & type(uint16).max > 0) {
        r -= 16;
    } else {
        x >>= 16;
    }
    if (x & type(uint8).max > 0) {
        r -= 8;
    } else {
        x >>= 8;
    }
    if (x & 0xf > 0) {
        r -= 4;
    } else {
        x >>= 4;
    }
    if (x & 0x3 > 0) {
        r -= 2;
    } else {
        x >>= 2;
    }
    if (x & 0x1 > 0) r -= 1;
}
```

---

# 🔥 First: What Does `leastSignificantBit()` Mean?

We already know:

```text
MOST SIGNIFICANT BIT
        ↓
highest-position 1
```

Now:

```text
LEAST SIGNIFICANT BIT
        ↓
lowest-position 1
```

For example:

```text
40 = 00101000
```

Number the bits from **right to left**:

```text
bit:    7 6 5 4 3 2 1 0
        -----------------
        0 0 1 0 1 0 0 0
            ↑   ↑
           bit5 bit3
```

There are two `1`s:

```text
bit 5
bit 3
```

The **least significant `1`** is:

```text
bit 3
```

because `3 < 5`.

So:

```solidity
leastSignificantBit(40)
```

returns:

```text
3
```

---

# 🧠 The First Big Difference From `mostSignificantBit()`

This is where we should pause.

For MSB, we were looking for:

```text
the highest-position 1
```

So visually:

```text
255 ─────────────────────────────────────────── 0
 ↑                                               ↑
highest side                                  lowest side

          ← find the first 1 from here
```

For LSB:

```text
255 ─────────────────────────────────────────── 0
 ↑                                               ↑
highest side                                  lowest side

                                               find the first 1 →
```

So:

```text
MSB = highest 1
LSB = lowest 1
```

Another way:

```text
MSB = leftmost 1
LSB = rightmost 1
```

when we write the binary number normally.

---

# 🧒 Child Analogy — 256 Light Bulbs

Imagine we have **256 light bulbs in a row**:

```text
255 254 253 ... 5 4 3 2 1 0
 ↑                         ↑
left                      right
```

Each bulb can be:

```text
0 = OFF
1 = ON
```

### MSB asks:

> “What is the first ON bulb if I search from the LEFT?”

### LSB asks:

> “What is the first ON bulb if I search from the RIGHT?”

For example:

```text
00010100
```

The ON bulbs are:

```text
bit 4
bit 2
```

MSB:

```text
bit 4
```

LSB:

```text
bit 2
```

Same binary number.

Different question.

---

# 🔥 Now Look at the Function Signature

```solidity
function leastSignificantBit(uint256 x) internal pure returns (uint8 r)
```

Let's break it down.

## `uint256 x`

`x` is the **actual number** we are inspecting.

It is not the bit position.

For example:

```solidity
leastSignificantBit(40)
```

means:

> “Look inside the number `40` and tell me the position of its lowest `1` bit.”

It does **not** mean:

> “Go look at bit position 40.”

This is exactly the same distinction we made with MSB.

---

# 🧩 In Our Tick Bitmap Flow

Remember where this function appears in the bigger picture.

We had:

```text
raw tick
   ↓
compressed tick
   ↓
wordPos + bitPos
   ↓
bitmap[wordPos]
   ↓
mask
   ↓
bitmap[wordPos] & mask
   ↓
masked
```

Then, depending on the direction, we use either:

```solidity
mostSignificantBit(masked)
```

or:

```solidity
leastSignificantBit(masked)
```

The important point is:

> **`masked` is already the filtered bitmap.**

This function does not create the mask.

It does not search the whole bitmap.

It receives the filtered `uint256` and finds the relevant lowest `1`.

For the higher-tick / `gt` search, that's exactly what we want.

---

# 🔥 What Is `leastSignificantBit()` Actually Returning?

It returns a number:

```text
0 → 255
```

representing:

> **the position of the lowest set `1` inside `x`.**

For example:

```text
x = 1

00000001
       ↑
      bit0
```

Return:

```text
0
```

---

```text
x = 2

00000010
      ↑
     bit1
```

Return:

```text
1
```

---

```text
x = 4

00000100
     ↑
    bit2
```

Return:

```text
2
```

---

```text
x = 12

00001100
    ↑ ↑
   3  2
```

The lowest `1` is bit 2.

Return:

```text
2
```

---

# 🔐 First Line

```solidity
require(x > 0);
```

Same reason as MSB.

If:

```text
x = 0
```

then:

```text
0000000000000000...
```

There isn't a single `1`.

Therefore:

> There is no least significant `1` to return.

So:

```text
x = 0
```

is invalid.

But:

```text
x = 1
```

is valid:

```text
00000001
       ↑
      bit0
```

---

# 🔥 Now The Weird Part: `r = 255`

Here is where LSB starts behaving differently from MSB.

```solidity
r = 255;
```

Why `255`?

Because a `uint256` has:

```text
256 bits
```

and therefore:

```text
bit positions:

0 → 255
```

So the **highest possible bit position** is:

```text
255
```

The function starts there.

But remember:

> We're looking for the **lowest** `1`.

So we're going to progressively move our candidate position **downward**.

Think:

```text
255
 ↓
127
 ↓
63
 ↓
31
 ↓
15
 ↓
7
 ↓
1
 ↓
0
```

Not every step necessarily happens, but this is the direction.

---

# 🧒 Think of `r` as Your Current Address

Imagine a building with rooms:

```text
255
254
253
...
3
2
1
0
```

You're told:

> “The answer is somewhere in this building.”

You initially have no clue, so your candidate upper boundary is:

```text
r = 255
```

Then you eliminate entire chunks.

If you discover:

> “The answer is in the lower 128 rooms.”

you say:

```text
255 - 128 = 127
```

So now:

```text
r = 127
```

You keep narrowing.

---

# 📦 The 256-Bit Box

Let's visualize the actual binary box:

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│                              256 BITS                                       │
├───────────────────────────────────────┬──────────────────────────────────────┤
│            BITS 255 → 128             │             BITS 127 → 0             │
│             UPPER 128                 │             LOWER 128                │
└───────────────────────────────────────┴──────────────────────────────────────┘
```

For LSB we ask:

> **Does the LOWER 128-bit half contain any `1`?**

Why?

Because if it contains a `1`, then the **lowest** `1` must be somewhere in that half.

We don't care about the upper half anymore.

---

# 🔥 First Real Check

```solidity
if (x & type(uint128).max > 0) {
    r -= 128;
} else {
    x >>= 128;
}
```

This looks much stranger than MSB.

MSB had:

```solidity
if (x >= 0x100000000000000000000000000000000)
```

LSB has:

```solidity
x & type(uint128).max
```

So let's understand exactly what this means.

---

# 🧩 `type(uint128).max`

A `uint128` has:

```text
128 bits
```

The maximum value is:

```text
1111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111111
```

That's:

```text
128 ones
```

So conceptually:

```text
type(uint128).max
=
128 ones
```

When viewed as a `uint256` mask:

```text
┌───────────────────────────────────────┬───────────────────────────────────────┐
│               128 zeros               │               128 ones               │
│                 ignore                │                 keep                  │
└───────────────────────────────────────┴───────────────────────────────────────┘
```

So this is a mask for the **lower 128 bits**.

---

# 🧠 Now `x & mask`

Remember AND:

```text
1 & 1 = 1
1 & 0 = 0
0 & 1 = 0
0 & 0 = 0
```

Therefore:

```text
x
&
0000000000000000 1111111111111111
```

keeps only the lower half.

Example with a tiny 8-bit box:

```text
x:

10110100

mask:

00001111

AND:

10110100
00001111
--------
00000100
```

The upper four bits disappear.

The lower four bits remain.

---

# 🔥 Then `> 0`

```solidity
x & type(uint128).max > 0
```

means:

> **After keeping only the lower 128 bits, is anything left?**

Or more simply:

> **Does the lower 128-bit region contain at least one `1`?**

There are two cases.

---

# 🟢 Case 1 — Lower Half Has a `1`

Imagine:

```text
┌───────────────────────────────┬───────────────────────────────┐
│           UPPER 128           │           LOWER 128           │
│                               │              1                │
└───────────────────────────────┴───────────────────────────────┘
```

Then:

```solidity
x & type(uint128).max
```

is greater than zero.

So:

```text
if (...)
```

is:

```text
TRUE
```

Therefore:

```solidity
r -= 128;
```

Starting from:

```text
r = 255
```

we get:

```text
255 - 128 = 127
```

Now:

```text
r = 127
```

This means:

> “We know the lowest `1` is somewhere in the lower 128-bit region.”

---

# 🔴 Case 2 — Lower Half Has No `1`

Now:

```text
┌───────────────────────────────┬───────────────────────────────┐
│           UPPER 128           │           LOWER 128           │
│              1                │              0                │
│                               │         all zeros             │
└───────────────────────────────┴───────────────────────────────┘
```

Then:

```text
x & type(uint128).max
```

equals:

```text
0
```

So:

```text
> 0
```

is false.

We go to:

```solidity
else {
    x >>= 128;
}
```

Meaning:

> “The lower half is completely empty. The lowest `1` must be in the upper half. Throw away the empty lower half and move the upper half down.”

---

# 💥 This Is The Big LSB Pattern

At every stage:

```text
                 CHECK LOWER HALF
                       │
             ┌─────────┴─────────┐
             │                   │
          HAS A 1             NO 1
             │                   │
             ↓                   ↓
         r -= chunk          x >>= chunk
             │                   │
             ↓                   ↓
       answer is inside     answer is inside
       lower half           upper half
```

This is the heart of the function.

---

# 🧠 Why Does `r -= 128` Instead of `r += 128`?

Because we're doing the opposite journey from MSB.

MSB:

```text
start = 0
move toward higher positions
r +=
```

LSB:

```text
start = 255
move toward lower positions
r -=
```

Visual:

```text
MSB

0 ───────────────────────────────────────── 255
↑
start
        → → → → →


LSB

255 ─────────────────────────────────────── 0
↑
start
        ← ← ← ← ←
```

So the arithmetic itself tells you the search direction.

---

# 🧒 Child Analogy — Cutting a Chocolate Bar

Imagine a chocolate bar representing all 256 bits.

```text
┌──────────────────────────────────────────────────────────────┐
│                         256 bits                             │
└──────────────────────────────────────────────────────────────┘
```

We want to find the **rightmost piece containing chocolate**.

First cut:

```text
┌─────────────────────────────┬───────────────────────────────┐
│          128                │              128              │
│          left               │              right             │
└─────────────────────────────┴───────────────────────────────┘
```

We inspect the right half.

If there is something there:

```text
KEEP RIGHT HALF
```

So our coordinate range becomes:

```text
255 → 127
```

That's why:

```solidity
r -= 128;
```

If the right half is empty:

```text
THROW RIGHT HALF AWAY
```

and inspect the left half:

```solidity
x >>= 128;
```

Then repeat.

---

# 🔥 Next: `type(uint64).max`

After the first narrowing, we perform:

```solidity
if (x & type(uint64).max > 0) {
    r -= 64;
} else {
    x >>= 64;
}
```

Same exact logic.

Now the remaining search area is effectively:

```text
128 bits
```

Split it:

```text
┌───────────────────────────────┬───────────────────────────────┐
│           UPPER 64            │           LOWER 64             │
└───────────────────────────────┴───────────────────────────────┘
```

We ask:

> **Does the lower 64 contain any `1`?**

If yes:

```solidity
r -= 64;
```

If no:

```solidity
x >>= 64;
```

---

# 🔥 Then `uint32`

```solidity
if (x & type(uint32).max > 0) {
    r -= 32;
} else {
    x >>= 32;
}
```

Now:

```text
64
↓
32 + 32
```

Check the lower 32.

---

# 🔥 Then `uint16`

```solidity
if (x & type(uint16).max > 0) {
    r -= 16;
} else {
    x >>= 16;
}
```

Now:

```text
32
↓
16 + 16
```

Check lower 16.

---

# 🔥 Then `uint8`

```solidity
if (x & type(uint8).max > 0) {
    r -= 8;
} else {
    x >>= 8;
}
```

Now:

```text
16
↓
8 + 8
```

Check lower 8.

---

# 🧠 Notice The Pattern

Look at these:

```solidity
type(uint128).max
type(uint64).max
type(uint32).max
type(uint16).max
type(uint8).max
```

These aren't random.

They are:

```text
128
 ↓
64
 ↓
32
 ↓
16
 ↓
8
```

Each time:

```text
half
```

of the current search region.

Then the function continues with:

```text
4
2
1
```

---

# 🔥 Now `0xf`

```solidity
if (x & 0xf > 0) {
    r -= 4;
} else {
    x >>= 4;
}
```

We know from our previous MSB discussion:

```text
0xf = 15
```

Binary:

```text
1111
```

So:

```text
0xf
 ↓
1111₂
 ↓
four 1s
 ↓
mask lower 4 bits
```

Our box is now:

```text
┌────┬────┬────┬────┐
│ b3 │ b2 │ b1 │ b0 │
└────┴────┴────┴────┘
```

The mask:

```text
1111
```

checks all four bits.

So:

```solidity
x & 0xf
```

asks:

> “Is there at least one `1` among these four bits?”

If yes:

```text
r -= 4
```

If no:

```text
x >>= 4
```

---

# 🔥 Then `0x3`

```solidity
if (x & 0x3 > 0) {
    r -= 2;
} else {
    x >>= 2;
}
```

We already know:

```text
0x3 = 3 = 11₂
```

So it masks the lower two bits:

```text
┌────┬────┐
│ b1 │ b0 │
└────┴────┘
```

And asks:

> **Does this lower 2-bit box contain any `1`?**

If yes:

```solidity
r -= 2;
```

If no:

```solidity
x >>= 2;
```

---

# 🔥 Finally `0x1`

```solidity
if (x & 0x1 > 0) r -= 1;
```

We know:

```text
0x1 = 1 = 1₂
```

So:

```text
0x1
 ↓
0001
```

It checks the final lowest bit.

In the final 2-bit mental model:

```text
A | B
```

where:

```text
A = bit 1
B = bit 0
```

The mask:

```text
01
```

checks **B**.

So:

```solidity
x & 0x1 > 0
```

is basically asking:

> **“Is B = 1?”**

If yes:

```text
least significant 1 = bit 0
```

and we subtract nothing.

If B is zero, then because:

```solidity
require(x > 0);
```

the remaining value cannot be zero.

So A must be `1`:

```text
10
↑
bit 1
```

and:

```solidity
r -= 1;
```

moves the answer from bit 1 to bit 0 only when necessary.

---

# 📦 Final 2-Bit Box

This is the same **A | B** model we used in MSB, but now we're interested in the right side first:

```text
bit 1 | bit 0
   A  |   B
```

Possible non-zero values:

```text
A | B
-----
0 | 1    = 01₂ = 1
1 | 0    = 10₂ = 2
1 | 1    = 11₂ = 3
```

For LSB:

```text
01
 ↑
B = 1
```

Answer:

```text
bit 0
```

For:

```text
10
↑
A = 1
```

answer:

```text
bit 1
```

For:

```text
11
```

B is still `1`, so the **least** significant `1` is:

```text
bit 0
```

This is important.

Even though `11` has a `1` at bit 1, we choose bit 0 because we're looking for the **lowest-position `1`**.

---

# 🧠 This Connects Directly to Your `x = 3` Question From MSB

You asked earlier:

> What if `x = 3`?

We had:

```text
3 = 11₂
```

For **MSB**:

```text
11
↑
bit 1
```

so MSB = 1.

For **LSB**:

```text
11
 ↑
bit 0
```

so LSB = 0.

Therefore:

```text
x = 3

MSB = 1
LSB = 0
```

Same number.

Different bit.

---

# 🔥 The Beautiful Mirror

```text
x = 3

        bit 1 | bit 0
             A | B
               |
        ┌──────┴──────┐
        │             │
       MSB           LSB
        │             │
       look A        look B
        │             │
        ↓             ↓
       bit 1         bit 0
```

This is probably the simplest way to remember the two functions.

---

# 🧠 One More Important Question

Why does LSB use:

```solidity
x & mask > 0
```

instead of simply asking:

```text
x >= something
```

Because the question is different.

MSB asks:

> “Is the highest `1` in this high region?”

LSB asks:

> **“Does this low region contain ANY `1`?”**

So LSB needs a mask:

```text
x
&
mask
↓
selected region
↓
> 0
↓
at least one 1?
```

This is why the code looks so different even though the two functions are conceptually related.

---

# 🔥 The Entire LSB Algorithm

Now we can read the entire function without memorizing individual lines:

```text
START
  │
  ↓
x must be > 0
  │
  ↓
r = 255
  │
  ↓
Does lower 128 contain a 1?
  │
  ├── YES → r -= 128
  │
  └── NO  → x >>= 128
  │
  ↓
Does lower 64 contain a 1?
  │
  ├── YES → r -= 64
  │
  └── NO  → x >>= 64
  │
  ↓
Does lower 32 contain a 1?
  │
  ├── YES → r -= 32
  │
  └── NO  → x >>= 32
  │
  ↓
Does lower 16 contain a 1?
  │
  ├── YES → r -= 16
  │
  └── NO  → x >>= 16
  │
  ↓
Does lower 8 contain a 1?
  │
  ├── YES → r -= 8
  │
  └── NO  → x >>= 8
  │
  ↓
Does lower 4 contain a 1?
  │
  ├── YES → r -= 4
  │
  └── NO  → x >>= 4
  │
  ↓
Does lower 2 contain a 1?
  │
  ├── YES → r -= 2
  │
  └── NO  → x >>= 2
  │
  ↓
Does final bit contain a 1?
  │
  └── YES → r -= 1
  │
  ↓
RETURN r
```

The fundamental question never changes:

> **“Is there a `1` in the lower half?”**

Only the size of the half changes:

```text
128
64
32
16
8
4
2
1
```

---

# 🧠 MSB vs LSB — Keep This Mental Picture

```text
                uint256
                   │
          ┌────────┴────────┐
          │                 │
         MSB               LSB
          │                 │
      highest 1          lowest 1
          │                 │
       start 0          start 255
          │                 │
        r +=             r -=
          │                 │
     search upward       search downward
```

And the final bits:

```text
A | B
-----
MSB → asks about A
LSB → asks about B
```

---

# 🔥 And Back to Tick Bitmap

This is where all of this becomes useful.

For the higher-direction search:

```text
current compressed tick
          ↓
position(compressed + 1)
          ↓
wordPos + bitPos
          ↓
mask
          ↓
bitmap[wordPos] & mask
          ↓
masked
          ↓
leastSignificantBit(masked)
```

The mask has already removed positions that are not allowed.

So now:

```text
masked
```

contains only possible initialized tick positions.

The **least significant `1`** inside this filtered value is the closest allowed initialized position in the higher direction.

So:

```text
GT search
   ↓
keep higher positions
   ↓
masked
   ↓
find lowest 1
   ↓
leastSignificantBit()
   ↓
closest initialized tick
```

That's why the LSB function exists in the Tick Bitmap algorithm.

---

# 🧠 The Core Thing to Remember

Don't memorize this:

```solidity
if (x & type(uint128).max > 0)
```

Instead translate it into English:

> **“Does the lower 128-bit box contain any `1`?”**

Then:

```text
YES
 ↓
answer is in lower half
 ↓
r -= 128
```

or:

```text
NO
 ↓
lower half is empty
 ↓
throw it away
 ↓
x >>= 128
```

Then repeat:

```text
128 → 64 → 32 → 16 → 8 → 4 → 2 → 1
```

That's the whole function.

And unlike MSB, where we **add** the skipped high-side positions, LSB starts at `255` and **subtracts** the eliminated high-side positions until `r` lands on the lowest set bit.

---
---
---
# 🧪Clarification with Example lets go(Mainly for me)

# 🧪 Example 1 — `x = 1`

Let's take the simplest possible valid input:

```text
x = 1

binary:

00000001
       ↑
      bit 0
```

We already know the answer:

```text
leastSignificantBit(1) = 0
```

But let's see how the actual function calculates it.

---

## 🔥 Start

```solidity
r = 255;
```

So:

```text
r = 255
x = 1
```

---

## ① Lower 128 Bits

The lower 128 bits contain the `1`.

Therefore:

```text
YES ✅
```

So:

```solidity
r -= 128;
```

Calculation:

```text
255 - 128 = 127
```

Now:

```text
r = 127
x = 1
```

---

## ② Lower 64 Bits

The lower 64 bits still contain the `1`.

```text
YES ✅
```

Therefore:

```solidity
r -= 64;
```

```text
127 - 64 = 63
```

Now:

```text
r = 63
x = 1
```

---

## ③ Lower 32 Bits

Again, the `1` is inside the lower 32 bits.

```text
YES ✅
```

```solidity
r -= 32;
```

```text
63 - 32 = 31
```

Now:

```text
r = 31
x = 1
```

---

## ④ Lower 16 Bits

The `1` is inside the lower 16 bits.

```text
YES ✅
```

```solidity
r -= 16;
```

```text
31 - 16 = 15
```

Now:

```text
r = 15
x = 1
```

---

## ⑤ Lower 8 Bits

The `1` is inside the lower 8 bits.

```text
YES ✅
```

```solidity
r -= 8;
```

```text
15 - 8 = 7
```

Now:

```text
r = 7
x = 1
```

---

## ⑥ Lower 4 Bits

The `1` is inside the lower 4 bits.

```text
YES ✅
```

```solidity
r -= 4;
```

```text
7 - 4 = 3
```

Now:

```text
r = 3
x = 1
```

---

## ⑦ Lower 2 Bits

The `1` is inside the lower 2 bits:

```text
01
 ↑
bit 0
```

So:

```text
YES ✅
```

Execute:

```solidity
r -= 2;
```

```text
3 - 2 = 1
```

Now:

```text
r = 1
x = 1
```

---

## ⑧ Final Bit

Now:

```solidity
if (x & 0x1 > 0)
    r -= 1;
```

We have:

```text
x = 1

01
 ↑
bit 0 = 1
```

Therefore:

```text
x & 0x1
=
1 & 1
=
1
```

So the condition is:

```text
TRUE ✅
```

Therefore:

```solidity
r -= 1;
```

Calculation:

```text
1 - 1 = 0
```

Final result:

```text
r = 0
```

Therefore:

```text
leastSignificantBit(1)
        ↓
       0
```

---

## 🔥 Entire Calculation At Once

```text
x = 1

r = 255

255 - 128 = 127
127 - 64  = 63
63  - 32  = 31
31  - 16  = 15
15  - 8   = 7
7   - 4   = 3
3   - 2   = 1
1   - 1   = 0

FINAL:
r = 0
```

So:

```text
leastSignificantBit(1) = 0
```

### 🧠 The Important Point

Even though `x` is already just `1`, the function **still starts `r` at `255`**.

It progressively narrows the search:

```text
255
 ↓
127
 ↓
63
 ↓
31
 ↓
15
 ↓
7
 ↓
3
 ↓
1
 ↓
0
```

Because the `1` is always inside the lower region, every check chooses:

```text
r -= chunk
```

until the search finally reaches:

```text
bit 0
```

---

# 🧪 Example 2 — `x = 2²²⁰`

Now let's take `x` with its only `1` at **bit 220**:

```text
x = 2²²⁰
```

So conceptually:

```text
bit position:

220
 ↓
1

all other bits = 0
```

Our goal is:

```text
leastSignificantBit(x) = 220
```

Now let's watch **`r` and `x` together**, because this is where the algorithm really clicks.

---

## 🔥 Start

```solidity
r = 255;
```

So:

```text
r = 255
x = 2²²⁰
```

The `1` is way up here:

```text
255 ─────────────────────── 220 ───────────────────────────── 0
                            ↑
                           1
```

---

## ① Check Lower 128 Bits

```solidity
if (x & type(uint128).max > 0)
```

We're asking:

> **Does the lower 128-bit region contain a `1`?**

But our `1` is at:

```text
bit 220
```

So:

```text
bits 127 → 0
```

are all zero.

Therefore:

```text
NO ❌
```

So we execute:

```solidity
x >>= 128;
```

We throw away the empty lower 128 bits.

Our `1` moves:

```text
220 - 128 = 92
```

So now:

```text
x = 2⁹²
r = 255
```

Notice something important:

### `r` did NOT change.

Because we discovered:

> The lower 128 bits were empty.

We didn't move the answer toward the lower side.

We simply **shifted `x` so we could inspect the upper region more easily**.

---

## ② Check Lower 64 Bits

Now:

```text
x = 2⁹²
```

So the remaining `1` is at:

```text
bit 92
```

We ask:

> Does the lower 64-bit region contain a `1`?

The lower region is:

```text
63 → 0
```

Our `1` is at:

```text
92
```

So:

```text
NO ❌
```

Therefore:

```solidity
x >>= 64;
```

Now the `1` moves:

```text
92 - 64 = 28
```

So:

```text
x = 2²⁸
r = 255
```

Again:

```text
r stays 255
```

because the lower 64 bits were empty.

---

## ③ Check Lower 32 Bits

Now:

```text
x = 2²⁸
```

The `1` is at:

```text
bit 28
```

Lower 32-bit region:

```text
31 → 0
```

Our `1` at bit 28 **is inside it**.

Therefore:

```text
YES ✅
```

So:

```solidity
r -= 32;
```

Therefore:

```text
r = 255 - 32
  = 223
```

And we **do not shift `x`**.

So:

```text
r = 223
x = 2²⁸
```

This is the key moment.

We've now established:

> The answer is inside the lower 32-bit region of our current search box.

---

## ④ Check Lower 16 Bits

Still:

```text
x = 2²⁸
```

The `1` is at bit 28.

Lower 16:

```text
15 → 0
```

Does it contain a `1`?

```text
NO ❌
```

So:

```solidity
x >>= 16;
```

The `1` moves:

```text
28 - 16 = 12
```

Now:

```text
x = 2¹²
r = 223
```

Again `r` doesn't change.

---

## ⑤ Check Lower 8 Bits

Now the `1` is at:

```text
bit 12
```

Lower 8:

```text
7 → 0
```

Does it contain a `1`?

```text
NO ❌
```

So:

```solidity
x >>= 8;
```

The `1` moves:

```text
12 - 8 = 4
```

Now:

```text
x = 2⁴
r = 223
```

---

## ⑥ Check Lower 4 Bits

Now:

```text
x = 2⁴
```

So:

```text
00010000
    ↑
   bit 4
```

The lower 4 bits are:

```text
bits 3 2 1 0
```

Our `1` is at:

```text
bit 4
```

Therefore:

```text
NO ❌
```

So:

```solidity
x >>= 4;
```

The `1` moves:

```text
4 - 4 = 0
```

Now:

```text
x = 1
r = 223
```

---

## ⑦ Check Lower 2 Bits

Now:

```text
x = 1
```

Binary:

```text
01
```

The lower 2 bits contain the `1`.

Therefore:

```text
YES ✅
```

So:

```solidity
r -= 2;
```

Therefore:

```text
r = 223 - 2
  = 221
```

---

## ⑧ Final Bit Check

Now:

```text
x = 1
```

And:

```solidity
x & 0x1 > 0
```

is:

```text
1 & 1 = 1
```

So:

```text
YES ✅
```

Execute:

```solidity
r -= 1;
```

Therefore:

```text
r = 221 - 1
  = 220
```

---

# 🎯 Final Answer

```text
leastSignificantBit(2²²⁰)
                ↓
               220
```

Exactly where our `1` was.

---

# 🔥 Put The Whole Calculation Together

```text
START

x = 2²²⁰
r = 255

lower 128?
    NO
    x >>= 128
    220 → 92
    r = 255

lower 64?
    NO
    x >>= 64
    92 → 28
    r = 255

lower 32?
    YES
    r -= 32
    255 → 223

lower 16?
    NO
    x >>= 16
    28 → 12
    r = 223

lower 8?
    NO
    x >>= 8
    12 → 4
    r = 223

lower 4?
    NO
    x >>= 4
    4 → 0
    r = 223

lower 2?
    YES
    r -= 2
    223 → 221

bit 0?
    YES
    r -= 1
    221 → 220

FINAL
    r = 220
```

And look at the chunks that `r` subtracted:

```text
32 + 2 + 1
```

Therefore:

```text
255 - 32 - 2 - 1
= 220
```

🔥 **This is the exact reason `r` starts at `255`: it represents the top endpoint, and every time the algorithm proves that the answer is in a lower half, it subtracts that half's size. When the answer is in the upper half, it shifts `x` instead, because the answer's original position hasn't moved in the coordinate system represented by `r`.**